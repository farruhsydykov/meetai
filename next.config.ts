import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['https://rosy-verena-unelaborately.ngrok-free.dev', 'localhost:3000']
};

export default nextConfig;
